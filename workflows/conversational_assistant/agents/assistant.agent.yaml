name: assistant
display_name: "Asistente Personal 💬"
description: "Personal assistant agent that understands natural language commands and executes actions across Todoist, YNAB, and Telegram."
model: gemini-2.0-flash-exp
input_schema:
  type: object
  properties:
    message:
      type: string
      description: "Natural language message from the user"
    telegram_chat_id:
      type: string
      description: "Telegram chat ID to reply to"
  required:
    - message
    - telegram_chat_id
# No output_schema — this is a ReAct agent that responds via tool calls
# (telegram.send_message_string), not structured JSON output.

